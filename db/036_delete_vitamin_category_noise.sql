-- ============================================================================
-- 036: 식약처 카테고리 코드 노이즈 삭제
-- canonical_name_ko LIKE '비타민/%' AND slug IS NULL → 37건
-- 식약처 건기식 분류 코드(예: "비타민/가르시니아캄보지아껍질추출물")가
-- 성분으로 잘못 임포트된 데이터. FK 참조 0건 확인 후 삭제.
-- ============================================================================

DELETE FROM ingredients
WHERE canonical_name_ko LIKE '비타민/%'
  AND slug IS NULL;
